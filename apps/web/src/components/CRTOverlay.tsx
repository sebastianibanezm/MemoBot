"use client";

/**
 * CRT Overlay Component
 * Adds scanlines and subtle flicker effect for tech-noir aesthetic
 */
export function CRTOverlay() {
  return <div className="crt-overlay" aria-hidden="true" />;
}
